"dimension:environment=development": {
  clients = [
    {
      id = websocket-client
      type = websocketclient
      ws-url = "ws://localhost:7777/servant-websocket"
    }
  ]
}

"dimension:environment=production": {
  clients = [
    //  {
    //    id = websocket-client
    //    type = websocketclient
    //    ws-url = "ws://localhost:"${servant.server.port}"/servant-websocket"
    //  }
  ]
}