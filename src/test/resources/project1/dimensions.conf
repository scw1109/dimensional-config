# Declare dimensions
dimensions = {
  # Dimension keys are the possible dimensions, items listed first have higher priority
  dimension-keys = [environment, network],
  available-dimensions {
    environment = [stage, production],
    network = [internal, external]
  }
}